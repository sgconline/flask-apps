apiVersion: v1
kind: Service
metadata:
  name: {{ include "contacts-app.fullname" . }}
  labels:
    {{- include "contacts-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "contacts-app.selectorLabels" . | nindent 4 }}
